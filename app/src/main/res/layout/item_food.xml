<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:padding="16dp">

    <!-- Food Name -->
    <TextView
        android:id="@+id/foodNameTv"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:text="Food Name"
        android:textSize="18sp"
        android:textStyle="bold"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintEnd_toStartOf="@+id/foodCaloriesTv"
        app:layout_constraintHorizontal_chainStyle="spread"/>

    <!-- Calories Number -->
    <TextView
        android:id="@+id/foodCaloriesTv"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="100"
        android:textSize="16sp"
        app:layout_constraintStart_toEndOf="@id/foodNameTv"
        app:layout_constraintTop_toTopOf="@id/foodNameTv"
        app:layout_constraintBottom_toBottomOf="@id/foodNameTv"/>

    <!-- Cal Label -->
    <TextView
        android:id="@+id/calLabel"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="cal"
        android:textSize="16sp"
        app:layout_constraintStart_toEndOf="@id/foodCaloriesTv"
        app:layout_constraintBaseline_toBaselineOf="@id/foodCaloriesTv"
        android:layout_marginStart="4dp"
        app:layout_constraintEnd_toEndOf="parent"/>

</androidx.constraintlayout.widget.ConstraintLayout>